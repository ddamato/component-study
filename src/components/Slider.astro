---
const { id, value = 0, step = 10 } = Astro.props;
---

<style>
  .slider {
    display: flex;
    align-items: center;
    gap: 1ch;
  }
</style>

<div class="slider">
  <label for={ id }><slot/></label>
  <input 
    type="range"
    id={ id }
    value={ value }
    step={ step }/>
</div>